/**
 * Override the Material-UI default theme palette.
 */
const customTheme = {
  // Override default theme's palette values (by defining a palette object)
  palette: {
    primary: {
      light: '#f9f5ff',
      main: '#8367b1',
      dark: '#5f4983',
    },
    secondary: {
      light: '#ffffff',
      main: '#e0e0e0',
      dark: '#aeaeae',
      contrastText: '#000',
    },
  },
};

export default customTheme;
